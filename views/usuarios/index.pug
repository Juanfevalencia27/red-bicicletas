extends ../layout

block content
    nav#mainNav2.navbar.navbar-expand-lg.navbar-dark.fixed-top
        .container
            a.navbar-brand.js-scroll-trigger(href='#page-top')
                img(src='/img/navbar-logo.svg' alt='')
                button.navbar-toggler.navbar-toggler-right(type='button' data-toggle='collapse' data-target='#navbarResponsive2' aria-controls='navbarResponsive' aria-expanded='false' aria-label='Toggle navigation')
                    | Menu
                    i.fas.fa-bars.ml-1
            #navbarResponsive2.collapse.navbar-collapse
                ul.navbar-nav.text-uppercase.ml-auto
                li.nav-item
                    a.nav-link.js-scroll-trigger(href='/usuarios/create') Nuevo usuario
                li.nav-item
                    a.nav-link.js-scroll-trigger(href='/') Home
    br
    br
    br

    table.table.table-striped.table-dark
        thead
            tr
                th(scope='col') #
                th(scope='col') Nombre
                th(scope='col') Email
                th(scope='col') Confirmado
                th(scope='col')
                th(scope='col') 
        tbody
            each objUsuario in obj1
                tr
                    th(scope='row')= objUsuario.id
                    td= objUsuario.nombre
                    td= objUsuario.email
                    td= objUsuario.verificado ? 'SI': 'NO'
                    td  
                       a(href='/usuarios/update/'+objUsuario.id) Actualizar usuario 
                    td
                        form.inline(method='post' action='/usuarios/delete/'+objUsuario.id)
                            input(type="hidden" name='id' value=objUsuario.id)
                            button.btn.btn-primary(type="submit" name='submit-params' value='submit')='Eliminar'


